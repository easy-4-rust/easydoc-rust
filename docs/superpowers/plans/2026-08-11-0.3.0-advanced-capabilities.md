# v0.3.0 高级能力（Phase 4）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: rust-module-layout, rust-testing

**Goal:** 实现 Roadmap Phase 4 的高级能力：OMML→LaTeX 完整转换、条件模板引擎、图片模板引擎、Markdown source map。

**Architecture:** `docs/easydoc-rust-Architecture.md`

**Tech Stack:** Rust 1.88+, quick-xml, regex

## Global Constraints

- MSRV 1.88.0
- `unsafe_code = "forbid"` 全 workspace
- pedantic clippy 全开 + `missing_docs = "warn"`
- 不得破坏现有 635+ 测试

---

### Task 1: OMML→LaTeX 完整转换

> Files:
> - Modify: `crates/easydoc-markdown/src/math/omml_to_latex.rs`
> - Modify: `crates/easydoc-markdown/src/math/latex_dict.rs`

**Steps:**
- [ ] 补齐 latex_dict.rs 中 TODO 标注的缺失符号
- [ ] 添加 `<m:eqArr>`（equation array）支持
- [ ] 添加 `<m:borderBox>`（border box）支持
- [ ] 添加 `<m:bar>`（bar/overline）支持
- [ ] 添加 `<m:groupChr>`（group character）支持
- [ ] 添加 `<m:phant>`（phantom）支持
- [ ] 验证覆盖率 >99%（对比 markitdown 原始符号表）
- [ ] 添加 10 个测试覆盖新增结构

---

### Task 2: 条件模板引擎

> Files:
> - Create: `crates/easydoc-template/src/conditional.rs`
> - Modify: `crates/easydoc-template/src/lib.rs`

**Steps:**
- [ ] 设计条件语法：`{{#if condition}}...{{else}}...{{/if}}`
- [ ] 实现条件解析器
- [ ] 支持比较运算符（== / != / > / < / >= / <=）
- [ ] 支持逻辑运算符（and / or / not）
- [ ] 支持嵌套条件
- [ ] 集成到 `fill_executor`
- [ ] 添加 8 个测试

---

### Task 3: 图片模板引擎

> Files:
> - Create: `crates/easydoc-template/src/image_template.rs`
> - Modify: `crates/easydoc-template/src/lib.rs`

**Steps:**
- [ ] 设计图片占位符语法：`{{image:path/to/image.png}}`
- [ ] 支持图片尺寸控制：`{{image:path;width=200;height=100}}`
- [ ] 支持图片对齐：`{{image:path;align=center}}`
- [ ] 支持图片替换（根据数据动态选择图片）
- [ ] 集成到 `fill_executor`
- [ ] 添加 5 个测试

---

### Task 4: Markdown Source Map

> Files:
> - Create: `crates/easydoc-markdown/src/source_map.rs`
> - Modify: `crates/easydoc-markdown/src/lib.rs`

**Steps:**
- [ ] 设计 source map 数据结构（Markdown 行号 → DOCX 位置）
- [ ] 实现 Markdown → DOCX 的 source map 生成
- [ ] 实现 DOCX → Markdown 的 source map 生成
- [ ] 支持段落级别的映射
- [ ] 支持表格单元格级别的映射
- [ ] 添加 5 个测试

---

### Task 5: Comments 和 Revision Tracking（读取）

> Files:
> - Create: `crates/easydoc-reader/src/extractor/comments.rs`
> - Modify: `crates/easydoc-reader/src/lib.rs`

**Steps:**
- [ ] 解析 `word/comments.xml`
- [ ] 提取评论内容（作者 / 日期 / 文本）
- [ ] 关联评论到文档位置（comment range start/end）
- [ ] 添加 `DocumentEvent::Comment` 事件
- [ ] 添加 3 个测试

---

### Task 6: Revision Tracking（读取）

> Files:
> - Create: `crates/easydoc-reader/src/extractor/revisions.rs`
> - Modify: `crates/easydoc-reader/src/lib.rs`

**Steps:**
- [ ] 解析 `word/document.xml` 中的修订标记（`<w:ins>` / `<w:del>`）
- [ ] 提取修订内容（作者 / 日期 / 插入/删除文本）
- [ ] 添加 `DocumentEvent::Revision` 事件
- [ ] 支持修订的接受/拒绝状态
- [ ] 添加 3 个测试

---

### Task 7: 语义模型扩展

> Files:
> - Modify: `crates/easydoc-core/src/document/document_block.rs`
> - Modify: `crates/easydoc-core/src/document/mod.rs`

**Steps:**
- [ ] 添加 `DocumentBlock::Comment { id, author, date, blocks }` 变体
- [ ] 添加 `DocumentBlock::Revision { revision_type, author, date, blocks }` 变体
- [ ] 更新 `DocumentContent` 的序列化/反序列化
- [ ] 添加 3 个测试

---

### Task 8: 集成测试与文档

> Files:
> - Modify: `crates/easydoc/tests/`
> - Modify: `docs/usage-guide.md`
> - Modify: `docs/roadmap.md`

**Steps:**
- [ ] 添加高级能力的集成测试
- [ ] 更新使用指南：新增条件模板 / 图片模板 / source map 章节
- [ ] 更新路线图：标记 Phase 4 完成
- [ ] 验证所有现有测试仍然通过

---

## Acceptance / Verification

```bash
cargo test --workspace                    # 所有测试通过
cargo clippy --workspace -- -D warnings   # 0 warnings
cargo fmt --check                         # 100% compliant
```

## 依赖关系

- 依赖 0.2.0 的 API 稳定性
- Task 1（OMML→LaTeX）独立
- Task 2-3（模板引擎）互相依赖
- Task 4（Source Map）独立
- Task 5-6（Comments/Revisions）互相依赖
- Task 7（语义模型）依赖 Task 5-6
