# v0.2.0 XLSX / PPTX / CSV 格式支持 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: rust-workspace, rust-module-layout

**Goal:** 新增 `easydoc-xlsx`（复用 easyexcel-rust）、`easydoc-pptx`、`easydoc-csv` 三个格式 crate，扩展 easydoc 门面 API。

**Architecture:** `docs/easydoc-rust-Architecture.md`

**Tech Stack:** Rust 1.88+, easyexcel-rust, quick-xml, csv, zip

## Global Constraints

- MSRV 1.88.0
- `unsafe_code = "forbid"` 全 workspace
- pedantic clippy 全开 + `missing_docs = "warn"`
- rustfmt 100% 合规
- 新 crate 必须有中英文 README

---

### Task 1: easydoc-xlsx crate 骨架

> Files:
> - Create: `crates/easydoc-xlsx/Cargo.toml`
> - Create: `crates/easydoc-xlsx/src/lib.rs`
> - Modify: `Cargo.toml`（workspace members）

**Steps:**
- [ ] 创建 crate，依赖 easyexcel-rust
- [ ] 定义 `XlsxReader` / `XlsxWriter` trait
- [ ] 定义 `XlsxSheet` / `XlsxRow` / `XlsxCell` 类型
- [ ] 实现 `XlsxBuilder`（sheet 选择 / 范围过滤 / 批量读取）

---

### Task 2: easydoc-xlsx 读取能力

> Files:
> - Create: `crates/easydoc-xlsx/src/reader.rs`
> - Create: `crates/easydoc-xlsx/src/builder.rs`

**Steps:**
- [ ] 复用 easyexcel-rust 的 XLSX 解析能力
- [ ] 实现 `read_sheet()` → `Vec<Vec<CellValue>>`
- [ ] 实现 `read_tables::<T>()` → `Vec<Vec<T>>`（类型化提取）
- [ ] 实现 `read_text()` → `String`（纯文本提取）
- [ ] 支持多 sheet 读取
- [ ] 添加 5 个测试

---

### Task 3: easydoc-xlsx 写入能力

> Files:
> - Create: `crates/easydoc-xlsx/src/writer.rs`

**Steps:**
- [ ] 实现 `write_sheet()` → XLSX 文件
- [ ] 实现 `TableWriteBuilder` 复用（从 `Vec<Struct>` 生成表格）
- [ ] 支持样式（字体 / 颜色 / 对齐 / 列宽）
- [ ] 支持多 sheet 写入
- [ ] 添加 5 个测试

---

### Task 4: easydoc-pptx crate 骨架

> Files:
> - Create: `crates/easydoc-pptx/Cargo.toml`
> - Create: `crates/easydoc-pptx/src/lib.rs`
> - Modify: `Cargo.toml`（workspace members）

**Steps:**
- [ ] 创建 crate，依赖 quick-xml + zip
- [ ] 定义 `PptxPresentation` / `PptxSlide` / `PptxShape` 类型
- [ ] 实现 `PptxBuilder`（幻灯片顺序 / 布局选择）

---

### Task 5: easydoc-pptx 读取能力

> Files:
> - Create: `crates/easydoc-pptx/src/reader.rs`

**Steps:**
- [ ] 解析 PPTX ZIP 结构（ppt/slides/slide1.xml）
- [ ] 提取文本内容（`<a:t>` 元素）
- [ ] 提取表格内容（`<a:tbl>` 元素）
- [ ] 提取图片（ppt/media/*）
- [ ] 添加 3 个测试

---

### Task 6: easydoc-pptx 写入能力

> Files:
> - Create: `crates/easydoc-pptx/src/writer.rs`

**Steps:**
- [ ] 实现基础幻灯片写入（标题 + 内容）
- [ ] 实现表格写入（复用 `DocxRow` trait）
- [ ] 实现图片插入
- [ ] 支持布局选择（标题幻灯片 / 内容幻灯片 / 空白）
- [ ] 添加 3 个测试

---

### Task 7: easydoc-csv crate 骨架

> Files:
> - Create: `crates/easydoc-csv/Cargo.toml`
> - Create: `crates/easydoc-csv/src/lib.rs`
> - Modify: `Cargo.toml`（workspace members）

**Steps:**
- [ ] 创建 crate，依赖 csv crate
- [ ] 定义 `CsvReader` / `CsvWriter` trait
- [ ] 定义 `CsvOptions`（delimiter / quote / encoding）

---

### Task 8: easydoc-csv 读取能力

> Files:
> - Create: `crates/easydoc-csv/src/reader.rs`

**Steps:**
- [ ] 实现 `read_records()` → `Vec<Vec<String>>`
- [ ] 实现 `read_tables::<T>()` → `Vec<T>`（类型化提取，复用 `DocxRow` trait）
- [ ] 支持自定义分隔符（逗号 / 制表符 / 分号）
- [ ] 支持 BOM 检测（UTF-8 / UTF-16）
- [ ] 添加 5 个测试

---

### Task 9: easydoc-csv 写入能力

> Files:
> - Create: `crates/easydoc-csv/src/writer.rs`

**Steps:**
- [ ] 实现 `write_records()` → CSV 文件
- [ ] 实现 `TableWriteBuilder` 复用（从 `Vec<Struct>` 生成表格）
- [ ] 支持自定义分隔符 / 引用字符
- [ ] 支持 header 行控制
- [ ] 添加 3 个测试

---

### Task 10: easydoc 门面 API 扩展

> Files:
> - Modify: `crates/easydoc/src/easy_doc.rs`
> - Modify: `crates/easydoc/src/lib.rs`

**Steps:**
- [ ] 新增 `EasyDoc::read_xlsx()` / `EasyDoc::write_xlsx()`
- [ ] 新增 `EasyDoc::read_pptx()` / `EasyDoc::write_pptx()`
- [ ] 新增 `EasyDoc::read_csv()` / `EasyDoc::write_csv()`
- [ ] 更新 `prelude` 模块
- [ ] 添加 6 个集成测试

---

### Task 11: 文档与示例

> Files:
> - Create: `crates/easydoc-xlsx/README.md` + `README_zh.md`
> - Create: `crates/easydoc-pptx/README.md` + `README_zh.md`
> - Create: `crates/easydoc-csv/README.md` + `README_zh.md`
> - Create: `examples/xlsx_read.rs`
> - Create: `examples/csv_read.rs`
> - Modify: `docs/usage-guide.md`
> - Modify: `docs/roadmap.md`

**Steps:**
- [ ] 为 3 个新 crate 编写中英文 README
- [ ] 编写 xlsx_read / csv_read 示例
- [ ] 更新使用指南：新增 XLSX / PPTX / CSV 章节
- [ ] 更新路线图：标记 Phase 5 部分完成

---

## Acceptance / Verification

```bash
cargo test --workspace                    # 所有测试通过
cargo clippy --workspace -- -D warnings   # 0 warnings
cargo fmt --check                         # 100% compliant
cargo build --examples                    # 示例编译通过
```

## 依赖关系

- 依赖 0.1.0 stable 的 API 冻结
- Task 1-3（XLSX）与 Task 4-6（PPTX）与 Task 7-9（CSV）互相独立
- Task 10 依赖 Task 1-9 完成
